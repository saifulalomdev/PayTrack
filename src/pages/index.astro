---
import { getTranslator } from "@/i18n/use-translation";
import MainLayout from "@/layouts/main.astro";
const locale = Astro.currentLocale ?? "en";
const t = getTranslator("bn", "auth");
---

<MainLayout>
    {t("login.title")}
</MainLayout>
